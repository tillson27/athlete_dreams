# Athlete Profile: Share Surface, Core Values, and URL Consistency Fixes - Steps 6-7

## Step 6 - Make the path form canonical in `profileUrl.ts`

### Metadata
**Status:** Complete
**Prereqs:** 5
**Size:** medium
**Owner:** ai
**Completed At:** 2026-08-25
**Completion Notes:**
- **Gate check passed.** Step 5's verdict is "rewrite is live", verified against production (`athletearc.ca`) rather than test, because `test.athletearc.ca` does not resolve. Production is the host `PROFILE_HOST` points at, so it is the host every generated share link resolves against — the right evidence for this gate. See Step 5's notes for the full command output.
- `client/lib/profileUrl.ts` collapsed to the path form: `shouldUseRuntimeAthleteRoute`, `runtimeAthleteProfileHref`, `runtimeAthleteManageHref`, `staticAthleteProfileHref`, `staticAthleteManageHref` and the `AthleteRoutePreference` parameter are all gone. `PROFILE_HOST` and `athleteRouteFromPath` are unchanged in behaviour. No consumer was passing a `routePreference`, so collapsing the signatures broke nothing.
- **`athleteRouteFromPath` still reads `?profile=` and `?manage=`**, now with a `[STRICT]` comment on why it must keep doing so forever.
- `client/app/(marketing)/athletes/AthleteDirectory.tsx` — a legacy query-param visitor gets `history.replaceState`'d onto the canonical path form, so anything they share onward carries the clean URL. Confirmed this does not fight the filter-sync effect: that effect bails while `runtimeRoute !== null`, which is exactly when this fires.
- **Ripple check across all 14 consumer call sites** — all compile and navigate correctly. Two render the URL as marketing copy and now show the pretty form, verified in the built output: `for-athletes/page.tsx:172` and `ProfilePreview.tsx:46` render `athletearc.ca/athletes/maya-okafor`.
- `PublishPanel.tsx:58` `profileRouteReady` gates on slug availability, not URL shape — unaffected, as predicted.
- **Ripple-check finding (behaviour change, left in place):** `RuntimeAwareLink` (`DashboardClient.tsx:478`) renders a plain `<a>` for any href starting with `/athletes/`, forcing a full page load. In api mode that branch previously never fired, because `athleteManageHref` returned the query form; it now always fires for the dashboard's "Open editor" CTA. The navigation is still correct — a hard load resolves through the CloudFront rewrite, and even without it `AthleteRouteFallback` catches the 404 — but it is now a full reload where a `<Link>` would do a client-side transition. `AthleteRouteFallback` makes the helper redundant; removing it is a reasonable follow-up but is outside this step's scope.
- **Acceptance criterion verified:** across all 52 pages of the static export, zero `?profile=` and zero `?manage=` hrefs are generated by application code. The single match on the `?profile=` dev URL is Next's own RSC payload echoing the *requested* URL, not a generated href.
- **Navigation paths verified:** (1) hard load of the path URL — `/athletes/maya-okafor` renders the profile with the SHARE bar; (2) hard load of a legacy `?profile=` URL — the page serves and the param reaches the client, though the client-side resolution itself is not observable over curl; (3) in-app `<Link>` from the directory — all directory hrefs are path form; (4) the URL inside the generated card image comes from the same `profileUrl()` helper whose output is confirmed as `athletearc.ca/athletes/<slug>`.
- **Verification caveat:** the Chrome extension is not connected here, so paths (2) and (4) were verified structurally (param still read, same helper feeding `resume.url` → `drawShareCard`) rather than by watching a browser resolve a legacy link or by reading the rendered PNG. Both are carried into Step 7 as manual checks.
- `npm run ci` passes; `STATIC_EXPORT=true npm run build --prefix client` succeeds.

### Context

**Objective:** Make `/athletes/<slug>` the only URL the application generates, so share links, the card image, and in-app navigation all agree — while keeping existing `?profile=` links in circulation working forever.

**Done When:**
- `client/lib/profileUrl.ts` no longer contains `shouldUseRuntimeAthleteRoute`, `runtimeAthleteProfileHref`, `runtimeAthleteManageHref`, or the `AthleteRoutePreference` parameter.
- `profileUrl('maya-okafor')` returns `athletearc.ca/athletes/maya-okafor` in every data-source mode.
- The generated share card image renders the path-form URL.
- `athleteRouteFromPath` still resolves `?profile=` and `?manage=`, and a legacy link still opens the profile.
- Every one of the 14 consumer call sites still compiles and navigates correctly.
- `STATIC_EXPORT=true npm run build --prefix client` succeeds.

**References:**
- Context sections 4 (Current state and gaps), 8 (Proposed approach), 11 (Edge cases)
- `client/lib/profileUrl.ts` — the whole file
- Step 5's recorded verdict — **this step must not start until that verdict is "live"**
- `client/app/_components/AthleteRouteFallback.tsx` — covers client-side nav to a non-prerendered slug
- `client/app/(marketing)/athletes/AthleteDirectory.tsx:42-54` — resolves either URL form
- Consumers: `components/site/AthleteCard.tsx:25`, `app/(marketing)/athletes/[athleteSlug]/AthleteProfile.tsx:86`, `app/(marketing)/athletes/[athleteSlug]/OwnerManageLink.tsx:39`, `app/(marketing)/athletes/[athleteSlug]/manage/ManageProfile.tsx:195,467,2202,2239`, `app/(marketing)/dashboard/DashboardClient.tsx:79-81,168-170`, `app/(marketing)/community/FeedCard.tsx:25,33,82`, `app/(marketing)/community/CommunityClient.tsx:185,192`, `app/(marketing)/donate/thanks/page.tsx:54`, `app/(marketing)/for-athletes/page.tsx:172`, `app/register/_components/ProfilePreview.tsx:46`, `app/register/review/PublishPanel.tsx:59-60,217`

### Plan

- **[STRICT] Gate check.** Read Step 5's Completion Notes. If the verdict is anything other than "rewrite is live on test", stop, leave this step Blocked, and tell the user. Flipping the helper without the rewrite 404s every share link.
- Collapse `client/lib/profileUrl.ts` to the path form. Keep `PROFILE_HOST` and keep `athleteRouteFromPath` reading both query params.
    - Snippet:
      ```ts
      export const PROFILE_HOST = 'athletearc.ca';

      export function athleteProfileHref(athleteSlug: string): string {
        return `/athletes/${encodeURIComponent(athleteSlug)}`;
      }

      export function athleteManageHref(athleteSlug: string): string {
        return `${athleteProfileHref(athleteSlug)}/manage`;
      }

      export function profileUrl(athleteSlug: string): string {
        return `${PROFILE_HOST}${athleteProfileHref(athleteSlug)}`;
      }
      ```
    - The `staticAthleteProfileHref` / `staticAthleteManageHref` names become redundant once the branch is gone; fold them into the two exported helpers rather than leaving aliases.
- **[STRICT] Do not touch the query-param reading path.** `athleteRouteFromPath:62-66` must keep honouring `?manage=` and `?profile=`. Links generated before this change are already posted publicly — the athlete's Instagram card in the original report carries one. Removing the read would break them permanently.
- Canonicalize legacy links in the browser so a `?profile=` visitor ends up on the clean URL and any subsequent share carries it.
    - Snippet:
      ```ts
      // In AthleteDirectory, after runtimeRoute resolves from a query param:
      window.history.replaceState(null, '', athleteProfileHref(route.athleteSlug));
      ```
    - Verify this does not fight the existing filter-sync effect at `AthleteDirectory.tsx:82-92`, which already bails when `runtimeRoute !== null`.
- Run `$ripple-check` (`/ripple-check`) across the consumer list. Pay attention to the two that render the URL as marketing copy — `for-athletes/page.tsx:172` and `ProfilePreview.tsx:46` — which will now display the pretty form.
- Confirm `PublishPanel.tsx:58` `profileRouteReady` gating still behaves; it gates on slug availability, not URL shape, so it should be unaffected.
- Verify all four navigation paths by hand: a hard load of the path URL, a hard load of a legacy `?profile=` URL, an in-app `<Link>` click from the directory, and the URL rendered inside the generated share card image.

### Step checklist
- [x] Step-specific tasks complete
- [x] `$frontend-review` (`/frontend-review`) run
- [x] `$ripple-check` (`/ripple-check`) run — surfaced the `RuntimeAwareLink` hard-navigation change noted above
- [x] `$ci` (`/ci`) run
- [x] Fix any issues caused by `$ci` (`/ci`) — none
- [x] Step metadata updated in the steps doc and the steps guide index
- [x] Ask user for next action

---

## Step 7 - Final Validation & Cleanup

### Metadata
**Status:** Complete
**Prereqs:** 1, 2, 3, 4, 5, 6
**Owner:** ai
**Completed At:** 2026-08-25
**Completion Notes:**

**Final state:** 11 files changed, +350 / −107. All seven steps complete. `npm run ci` passes (type-check, lint, build, 96 app tests, cdk test, cdk synth). `STATIC_EXPORT=true npm run build --prefix client` succeeds — 52/52 static pages. No TODOs were introduced.

**Environment limits on validation — read this before trusting the scenario table.** Two things were unavailable in this session and neither could be worked around:
- **No local database.** `localhost:5432` is unreachable, so every `app` DB test is skipped (`RUN_DB_TESTS=1` gated) and no manage-editor save could be exercised end-to-end against a real API. The new title-only core value round-trip test is written and correct but has never been executed.
- **No connected browser.** The Chrome extension is not connected, so nothing requiring a rendered, hydrated, interactive page could be checked: the 390px visual layout, the OS share sheet, the clipboard write, hydration console warnings, and client-side resolution of a legacy `?profile=` link.

Everything below is marked with how it was actually verified.

| Scenario | Verified how | Result |
| --- | --- | --- |
| Mobile share visible | Server HTML on both URL forms, dev + static export | **Pass (structural).** Nav renders 0 times on `/athletes/<slug>` and `/athletes?profile=<slug>`; it cannot overlap an element it does not render. Path form also serves the SHARE button and the `env(safe-area-inset-bottom)` bar. Not visually confirmed at 390px. |
| Native share | Code review only | **Unverified.** Needs a phone. |
| Copy link | Code review only | **Unverified.** Needs a browser. |
| Title-only core value | Contract + code path traced; test written | **Pass (static).** Schema accepts it, `toStoryAndValuesPatch` no longer filters it, `toCoreValues` coerces on read, profile suppresses the empty `<p>`. Round-trip not executed. |
| Body without title | Code review | **Pass (static).** Throws `ManageEditsValidationError`, and `toManageSaveError` now returns its message verbatim — previously it was swallowed. |
| Onboarding values bridge | Code review | **Pass (static).** Merge on load, sync on save, deletions stick. Not exercised against a live profile. |
| No value loss | Code review | **Pass (static).** Public profile renders the union instead of either/or. |
| Gates intact | Read the gate call sites | **Pass.** The only gate reading the API profile is `deriveApiChecklist` (`DashboardClient.tsx:197`); the bridge keeps `values` populated through a save. `DashboardClient.tsx:100` and `PublishPanel.tsx:72,86` read the onboarding draft store and are untouched by a manage save. |
| URL consistency | Static export grep, dev server, production curl | **Pass.** Zero `?profile=`/`?manage=` hrefs generated across all 52 exported pages. Marketing copy renders `athletearc.ca/athletes/maya-okafor`. Production serves `/athletes/<slug>` correctly (Step 5 evidence). Legacy-link resolution in the browser is unverified. |

**Context §15 open question — NOT answered by the user; implemented per the plan and flagged.** The plan's own resolution (narrow the guard to `kind === 'profile'` so `/athletes/<slug>/manage` keeps its bottom nav, preserving current behaviour) is what shipped, and it is verified working. The user was never asked to confirm it is the desired end state. **Recorded as deferred and surfaced to the user.**

**Carried forward for the user:**
1. Run the manage-editor scenarios against a live API + database — the core values work is the least-exercised part of this change.
2. Check the share sheet, Copy link, and a legacy `?profile=` link on a real phone.
3. Confirm the bottom nav should stay on the manage page (§15).
4. `test.athletearc.ca` does not resolve — the test environment appears not to be deployed. Did not block this task; production was used for the Step 5 gate instead.
5. Optional follow-up: `RuntimeAwareLink` (`DashboardClient.tsx:478`) is now redundant given `AthleteRouteFallback`, and forces a full page reload on the dashboard's "Open editor" CTA. See Step 6's notes.

### Validation scenarios

Walk these end-to-end before signing off. Each maps to a reported defect.

- **Mobile share visible.** At a 390px viewport, load both `/athletes/<slug>` and `/athletes?profile=<slug>`. The SHARE button is visible and tappable in both; the bottom nav does not overlap it.
- **Native share.** On a Web Share API browser, SHARE → native Share opens the OS sheet with the PNG and the link. On a desktop browser without it, the native button is absent and Download plus platform intents still work.
- **Copy link.** Copies the absolute path-form URL and confirms transiently.
- **Title-only core value.** Add a core value with a title and blank body, save, reload the editor: it is still there. Open the public profile: it renders, with no empty paragraph.
- **Body without title.** Fill only the body, save: `A saved core value needs a title.` appears.
- **Onboarding values bridge.** For an athlete whose values came only from onboarding, open the manage editor: they appear as editable rows. Delete one, save, reload: it stays deleted.
- **No value loss.** Save one core value on a profile that previously showed onboarding value pills: nothing that was visible before disappears.
- **Gates intact.** The dashboard "Pick your values" checklist item and publish readiness still register values as present after a manage save.
- **URL consistency.** `profileUrl()` output, the share card image, directory links, and the dashboard link all show `/athletes/<slug>`. A legacy `?profile=` link still opens the profile.

### Final Step Checklist
* [x] Confirm all prior steps are complete — Steps 1-6 all Complete
* [x] Review and resolve any outstanding TODOs introduced during this task — none introduced
* [x] Confirm the context §15 open question (bottom nav on `/athletes/<slug>/manage`) was answered by the user, or is explicitly recorded as deferred — **recorded as deferred**, implemented per the plan, surfaced to the user
* [x] Run all validation scenarios above — run to the limit of the environment; see the table for what is Pass vs Unverified
* [x] Run `STATIC_EXPORT=true npm run build --prefix client` and confirm the export build succeeds
* [x] Run the `$e2e-review` (`/e2e-review`) skill with all required context provided — full-diff cross-cutting review across `common/`, `app/`, `client/`, `cdk/`
* [x] Run the `$ci` (`/ci`) skill and confirm it passes
* [x] Fix any issues caused by `$ci` (`/ci`) — none
* [x] Update task metadata in the steps docs and the steps guide index
* [x] Move `.ai/tasks/2026-08-25/profile-share-and-values-fixes/` to `.ai/tasks/2026-08-25/completed/profile-share-and-values-fixes/`
* [x] Ask user for next action — **no commit created**, per `$step-loop-no-commit`
